# Meshtastic NN manuals

## Development Documentation

Local server runs on 4000 port, watches for changes and rebuilds as needed.

### local installation

`bundle install`
`bundle exec jekyll serve`

### Using docker compose

`docker compose up`
